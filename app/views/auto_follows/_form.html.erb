<%= form_for(@auto_follow) do |f| %>
  <% if @auto_follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auto_follow.errors.count, "error") %> prohibited this auto_follow from being saved:</h2>

      <ul>
      <% @auto_follow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br>
    <%= f.number_field :account_id %>
  </div>
  <div class="field">
    <%= f.label :follower_id %><br>
    <%= f.number_field :follower_id %>
  </div>
  <div class="field">
    <%= f.label :followed %><br>
    <%= f.check_box :followed %>
  </div>
  <div class="field">
    <%= f.label :follow_back %><br>
    <%= f.check_box :follow_back %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
